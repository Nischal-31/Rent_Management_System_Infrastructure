apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: postgres-pvc
  namespace: rent-management-system

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 10Gi